export default async function AdminHomePage() { // Renamed component for clarity, optional
  return (
    <main className="flex min-h-screen flex-col items-center justify-between p-24">
      {/* Conteúdo do Dashboard do Admin virá aqui */}
    </main>
  );
}
